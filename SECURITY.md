# 安全策略

## 支持版本

当前维护 `main` 分支和最新 release。旧版本不提供安全修复保证。

## 报告安全问题

请不要在公开 Issue 中披露漏洞。请通过仓库维护者私下联系，并包含：

- 影响版本
- 复现步骤
- 影响范围
- 可能的修复建议

## 凭据与敏感数据

以下内容不得提交：

- API Key、Token、Webhook Secret
- `.env` 文件
- 生产数据库
- 代理账号
- Agent 截图中的敏感信息

## 依赖与供应链

- CI 会运行测试和 Docker 构建。
- Release 前必须确认版本号、CHANGELOG 和测试通过。
- 外部数据源解析失败时不得静默覆盖正式数据。
